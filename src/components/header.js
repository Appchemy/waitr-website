import React from 'react'
import { Theme } from '../styles/theme'

const Header = () => {
    return (
        <div style={{width: '100%', background: Theme.primaryColor, height: 600}}>

        </div>
    )
}

export default Header